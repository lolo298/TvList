services:
  # web:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   ports:
  #     - '5173:5173'
  #   environment:
  #     - NODE_ENV=production
  #   volumes:
  #     - ./src:/app/src
  #   depends_on:
  #     - db
  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: tvlist
      POSTGRES_PASSWORD: tvlist
      POSTGRES_DB: tvlist
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
